# JobBot does not require cloud API keys for the MVP.
# Optional LLM path (jobbot[llm]): used only inside promoted functions via --llm.
# JOBBOT_LOG_LEVEL=INFO
# OPENAI_API_KEY=
# JOBBOT_LLM_MODEL=gpt-4o-mini
